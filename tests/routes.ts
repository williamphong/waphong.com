// The route list every cross-cutting spec walks (metadata, axe, links). Kept
// in one place so a new route cannot be added to one spec and missed by the
// others.
export const routes = [
  '/',
  '/experience',
  '/projects',
  '/gallery',
  '/blog',
  '/blog/welcome-to-my-blog',
];
